# G.I. Backend

Lightweight Node.js backend for the G.I. global AI platform.

## Scripts
- `npm start` — start server
- `npm run dev` — start in development mode
- `npm test` — run tests

## Structure
- `src/` — application source
- `test/` — tests
- `.env` — environment variables
